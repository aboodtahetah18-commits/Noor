# Namaa App P0.4.30 source import

Source artifact: `Namaa_App_P0.4.30_BRAND_FINAL.zip`.

This branch is a safe overlay on the existing Noor repository. Existing repository-only files are preserved. Production database migrations, RLS policy changes, grants, and ownership migration scripts from the artifact are intentionally excluded from this import and are not executed. No production database or Neon main changes are part of this branch.

The protected transparent brand assets from the artifact remain the intended UI logo source. No generated or guessed replacement logo is introduced.
