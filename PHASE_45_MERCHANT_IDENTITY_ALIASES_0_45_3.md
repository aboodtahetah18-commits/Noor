# P45.3 — Merchant Identity & Aliases

## الهدف
السماح للتاجر الواحد بامتلاك اسم موحد وعدة مسميات بنكية/تشغيلية/فروع/فرنشايز، مع إبقاء التصنيف والقاعدة المالية موحدة.

## قواعد التنفيذ
- `merchant_rules` يمثل هوية التاجر الموحدة.
- `merchant_rule_aliases` يمثل المسميات البديلة المعتمدة من المستخدم.
- لا يدمج النظام اسمًا مختلفًا بتاجر آخر تلقائيًا عند وجود تعارض.
- جميع المسميات النشطة تدخل محرك المطابقة وتعيد نفس `merchant_rule_id`.
- عند مراجعة عملية تطابق تاجرًا معروفًا ثم اختيار «تذكّر»، يحفظ المسمى الجديد كـ alias بدل إنشاء تاجر مكرر.
- يمكن إيقاف/إعادة تفعيل أي alias دون حذف التاريخ.
- التصنيف والحساب المقابل ومستوى الاعتماد تبقى على هوية التاجر الموحدة.

## Migration
`20260903_046_merchant_aliases.sql`
