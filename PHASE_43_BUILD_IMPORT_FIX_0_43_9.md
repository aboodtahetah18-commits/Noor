# P43 Build Import Fix — v0.43.9

- Fixed stale account edit imports in the delivered ZIP.
- `accountNameOptions` replaced with `ACCOUNT_NAME_PRESETS`.
- `bankNames` replaced with `BANK_OPTIONS.map(...)`.
- Audited named local imports across `src`.
- Version bumped to 0.43.9.
