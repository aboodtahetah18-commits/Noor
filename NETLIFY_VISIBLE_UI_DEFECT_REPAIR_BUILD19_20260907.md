# Build 19 — Visible UI Defect Repair

Repairs based on live screenshots:
- Rebuilt default light/dark logo assets from the valid first logo only; removes duplicated/cropped artwork.
- Profile trigger fallback is now a real user icon instead of a duplicate app/chart mark.
- Modal/profile/security forms are constrained to the dialog, two columns on desktop and one column on mobile.
- File input, email/password fields, and action buttons no longer overflow or stack characters vertically.
- Cycle back action is a compact visible icon button rather than an empty dark rectangle.
- Compact filter content remains fully hidden until the filter button is pressed.
- Removed the native title tooltip from the filter toggle.
