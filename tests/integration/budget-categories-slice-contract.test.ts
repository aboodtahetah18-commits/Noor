import {describe,it,expect} from 'vitest';import fs from 'node:fs';import path from 'node:path';const read=(f:string)=>fs.readFileSync(path.join(process.cwd(),f),'utf8');
describe('budget categories slice contract',()=>{it('scopes repository operations by user ownership',()=>{const s=read('src/repositories/budget-category-repository.ts');expect(s.match(/user_id=\$\{userId\}/g)?.length??0).toBeGreaterThanOrEqual(3)});it('deactivates instead of deleting',()=>{const s=read('src/repositories/budget-category-repository.ts');expect(s).toContain('set is_active=false');expect(s).not.toMatch(/delete from public\.budget_categories/i)});it('has documented default seed categories',()=>{const s=read('src/repositories/budget-category-repository.ts');for(const n of ['غذاء','وقود','اتصالات','مواصلات','مطاعم','احتياجات منزلية','مصروف شخصي'])expect(s).toContain(n)})});
