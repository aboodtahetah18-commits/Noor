import {describe,it,expect} from 'vitest'; import { budgetCategoryInputSchema } from '@/features/budget-categories/schemas/budget-category';
describe('budget category validation',()=>{it('rejects UNPLANNED as default nature',()=>{expect(budgetCategoryInputSchema.safeParse({name:'مطاعم',categoryGroup:'FLEXIBLE',expenseNatureDefault:'UNPLANNED',isEssential:false}).success).toBe(false)});it('accepts documented groups',()=>{expect(budgetCategoryInputSchema.safeParse({name:'وقود',categoryGroup:'ESSENTIAL',expenseNatureDefault:'NECESSARY',isEssential:true}).success).toBe(true)})});
