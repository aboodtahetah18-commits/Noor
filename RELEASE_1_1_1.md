# Release 1.1.1

P60 TypeScript compatibility repair for the profile avatar server action.

- Explicit non-returning redirect helper typing fixes `FormDataEntryValue` → `File` control-flow narrowing.
- No functional or database changes.
- Migration inventory remains 65.
