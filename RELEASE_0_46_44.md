# Release 0.46.44 — Netlify TypeScript Strict Repair

Build-stability hotfix only. No financial behavior changes.

Primary target: Netlify `Failed to type check` errors involving nullable/undefined values under strict TypeScript and `noUncheckedIndexedAccess`.
