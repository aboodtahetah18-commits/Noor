import { rawSql } from '@/infrastructure/db/client';
export async function reconcilePriorMessages(userId:string,importId:string,accountId:string){const rows=await rawSql`select r.id,r.transaction_date::text as "transactionDate",r.transaction_time::text as "transactionTime",r.amount::text,r.direction,r.normalized_merchant as "normalizedMerchant" from public.bank_statement_rows r where r.user_id=${userId} and r.import_id=${importId} order by r.row_number`;let matched=0;for(const row of rows){if(!row.transactionDate)continue;const candidates=await rawSql`select p.id,p.final_transaction_id as "finalTransactionId",p.category_id as "categoryId",p.merchant_rule_id as "merchantRuleId",p.detected_kind as "detectedKind" from public.bank_statement_rows p join public.bank_statement_imports pi on pi.id=p.import_id and pi.user_id=p.user_id where p.user_id=${userId} and pi.account_id=${accountId} and pi.file_type='MESSAGE' and p.transaction_date=${row.transactionDate}::date and p.amount=${row.amount}::numeric and p.direction=${row.direction} and coalesce(p.normalized_merchant,'')=coalesce(${row.normalizedMerchant},'') and p.id<>${row.id} and (${row.transactionTime??null}::time is null or p.transaction_time=${row.transactionTime??null}::time) order by p.created_at desc limit 3`;if(candidates.length!==1)continue;const p=candidates[0];if(!p)continue;await rawSql`update public.bank_statement_rows set prior_message_row_id=${p.id},reconciliation_source='PRIOR_MESSAGE',category_id=coalesce(category_id,${p.categoryId??null}),merchant_rule_id=coalesce(merchant_rule_id,${p.merchantRuleId??null}),detected_kind=case when detected_kind='UNKNOWN' then ${p.detectedKind} else detected_kind end,matched_transaction_id=coalesce(${p.finalTransactionId??null},matched_transaction_id),duplicate_candidate=true,review_status='IGNORED',approval_action=case when ${p.finalTransactionId??null}::uuid is not null then 'MATCHED_EXISTING' else approval_action end,final_transaction_id=coalesce(${p.finalTransactionId??null},final_transaction_id),decision_source='DUPLICATE_MATCH',duplicate_score=100,decision_reason='مطابقة رسالة بنكية سابقة؛ كشف الحساب للإثراء فقط ومنع تكرار الأثر المالي',updated_at=now() where id=${row.id} and user_id=${userId}`;matched++}return matched}
