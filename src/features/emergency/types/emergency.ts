import type { EmergencyFundStatus, EmergencyAllocationStatus } from '@/domain/types';
export interface EmergencyFundSummary { emergencyFundId:string; name:string; targetAmount:string|null; currentBalance:string; progressPercent:string|null; status:EmergencyFundStatus; allocation:null|{id:string;plannedAmount:string;allocatedAmount:string;actualContributedAmount:string;remainingToTransfer:string;status:EmergencyAllocationStatus}; coverageMonths:string|null; coverageMonthsStatus:'AVAILABLE'|'UNAVAILABLE_NO_ESSENTIAL_BASELINE'; targetPolicy:'USER_DEFINED_AMOUNT'; }
export interface EmergencyMovementResult { movementId:string; outTransactionId:string; inTransactionId:string; amount:string; movementType:'CONTRIBUTION'|'WITHDRAWAL'; currentBalance:string; fundStatus:EmergencyFundStatus; totalLiquidityChange:'0.00'; postedAt:string; }
