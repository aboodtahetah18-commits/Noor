import { createFinancialPlanSchema } from '../schemas/financial-plan'; import { financialPlanRepository } from '@/repositories/financial-plan-repository';
export async function createPlanDraft(userId:string,input:unknown){const p=createFinancialPlanSchema.safeParse(input);if(!p.success)return{success:false as const,code:'VALIDATION_ERROR',message:p.error.issues[0]?.message??'الخطة غير صالحة'};try{return{success:true as const,planId:await financialPlanRepository.createDraft(userId,p.data)}}catch(e){const m=e instanceof Error?e.message:'';return{success:false as const,code:m.includes('unique')?'CONFLICT':'DATABASE_ERROR',message:m==='INVALID_ALLOCATION_REFERENCE'?'يوجد بند غير صالح في الخطة':'تعذر إنشاء الخطة'}}}
