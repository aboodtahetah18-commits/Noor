import { rawSql } from '@/infrastructure/db/client';
export async function getTripHistoryForCity(userId:string,destinationCity:string){const city=destinationCity.trim();if(!city)return{destinationCity:city,completedTrips:0,benchmarkTrips:0,averageAmount:null,minimumAmount:null,maximumAmount:null,trips:[],categories:[]};const trips=await rawSql`select e.id,e.title,e.starts_at as "startsAt",e.ends_at as "endsAt",e.planned_amount::text as "plannedAmount",e.benchmark_eligible as "benchmarkEligible",e.benchmark_note as "benchmarkNote",coalesce(sum(l.amount),0)::text as "actualAmount" from public.goal_events e left join public.goal_event_transaction_links l on l.goal_event_id=e.id and l.user_id=e.user_id where e.user_id=${userId} and e.event_type='TRIP' and e.status='CLOSED' and lower(trim(coalesce(e.destination_city,'')))=lower(trim(${city})) group by e.id order by e.starts_at desc nulls last,e.created_at desc`;const bRows=await rawSql`select coalesce(avg(x.actual_amount),0)::text avg,coalesce(min(x.actual_amount),0)::text min,coalesce(max(x.actual_amount),0)::text max,count(*)::int count from(select e.id,coalesce(sum(l.amount),0)::numeric actual_amount from public.goal_events e left join public.goal_event_transaction_links l on l.goal_event_id=e.id and l.user_id=e.user_id where e.user_id=${userId} and e.event_type='TRIP' and e.status='CLOSED' and e.benchmark_eligible=true and lower(trim(coalesce(e.destination_city,'')))=lower(trim(${city})) group by e.id)x`;const b=bRows[0]??{},count=Number(b.count??0);const cats=count?await rawSql`select l.category_id as "categoryId",coalesce(c.name,'غير مصنف') as "categoryName",(sum(l.amount)/${count})::numeric(18,2)::text amount from public.goal_events e join public.goal_event_transaction_links l on l.goal_event_id=e.id and l.user_id=e.user_id left join public.budget_categories c on c.id=l.category_id and c.user_id=l.user_id where e.user_id=${userId} and e.event_type='TRIP' and e.status='CLOSED' and e.benchmark_eligible=true and lower(trim(coalesce(e.destination_city,'')))=lower(trim(${city})) group by l.category_id,c.name order by sum(l.amount) desc`:[];return{destinationCity:city,completedTrips:trips.length,benchmarkTrips:count,averageAmount:count?String(b.avg):null,minimumAmount:count?String(b.min):null,maximumAmount:count?String(b.max):null,trips:trips.map(r=>({id:String(r.id),title:String(r.title),startsAt:r.startsAt?new Date(String(r.startsAt)).toISOString():null,endsAt:r.endsAt?new Date(String(r.endsAt)).toISOString():null,plannedAmount:r.plannedAmount?String(r.plannedAmount):null,actualAmount:String(r.actualAmount??'0'),benchmarkEligible:Boolean(r.benchmarkEligible),benchmarkNote:r.benchmarkNote?String(r.benchmarkNote):null})),categories:cats.map(r=>({categoryId:r.categoryId?String(r.categoryId):null,categoryName:String(r.categoryName),amount:String(r.amount??'0')}))}}
