import { notFound } from 'next/navigation';import { requireAuthenticatedUser } from '@/auth/require-authenticated-user';import { getGoal } from '@/features/goals/queries/get-goal';import { listAccounts } from '@/features/accounts/queries/list-accounts';import { contributeGoalAction } from '../../actions';
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params,u=await requireAuthenticatedUser(),g=await getGoal(u.id,id);if(!g)notFound();const goal=g as NonNullable<typeof g>;const accounts=await listAccounts(u.id,false),today=new Date().toISOString().slice(0,10);return <main className="page-shell p47-flow-page" dir="rtl"><header className="page-header p47-flow-header"><div><p className="eyebrow">مساهمة هدف</p><div className="title-with-help"><h1>{goal.name}</h1></div></div></header><form className="card p47-flow-card p47-flow-form form-grid" action={contributeGoalAction}><input type="hidden" name="goalId" value={goal.id}/><label>الحساب<select name="accountId" required><option value="">اختر الحساب</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select></label><label>المبلغ<input name="amount" inputMode="decimal" required/></label><label>التاريخ<input name="transactionDate" type="date" required defaultValue={today}/></label><button type="submit">تسجيل المساهمة</button></form><section className="card p47-flow-card"><p className="muted">هذه المساهمة تخصم المبلغ من الحساب المحدد وتضيفه إلى رصيد الهدف. لا تُحتسب كمصروف استهلاكي.</p></section></main>}
