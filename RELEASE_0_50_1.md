# Release 0.50.1

P50.1 Netlify ESLint zero-warning repair.

- Fixes the 10 `no-unused-expressions` warnings introduced by the P50 acceptance verifier.
- Preserves all P50 acceptance checks.
- No database migration; inventory remains 64.
